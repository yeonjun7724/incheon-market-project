# ─────────────────────────────────────────────
# LocalCart — requirements.txt
# Python 3.10+ 권장
# ─────────────────────────────────────────────

# ── 핵심 프레임워크 ──
streamlit==1.45.1

# ── 지도·공간분석 ──
folium==0.19.7
streamlit-folium==0.25.3
geopandas==1.0.1
shapely==2.0.6

# ── 데이터 처리 ──
pandas==2.2.3
numpy==1.26.4

# ── 수치 최적화 (LP 기반 품목 선정) ──
scipy==1.13.1

# ── 시각화 ──
plotly==5.24.1

# ── HTTP 클라이언트 (공공 API 연동) ──
requests==2.32.3

# ── 환경변수 관리 ──
python-dotenv==1.0.1
